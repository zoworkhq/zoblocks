# Copy to .env.local and fill in. Never commit the filled version.

# Pooled Postgres connection string. Neon and Supabase both expose one —
# use the POOLED url, not the direct one: every serverless invocation is its
# own process, and direct connections exhaust the server's limit quickly.
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
