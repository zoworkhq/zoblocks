node_modules
# Local agent worktrees: whole checkouts of other branches, never committed here.
.claude
.next
dist
.turbo
pnpm-lock.yaml
apps/docs/public/r
apps/docs/public/schema

# Machine-written. Formatting these would fight the tool that produces them:
# Prettier reformats, the next `pnpm gen` or `drizzle-kit generate` writes it
# back, and CI's stale-generated-files check fails on a diff nobody made.
# The generators own their own output shape.
registry.json
tsconfig.generated.json
apps/docs/src/lib/generated
apps/docs/src/app/generated-sources.css
packages/react/src
apps/hq/drizzle/meta

# Token build outputs. The DTCG source in packages/tokens/tokens/ is
# hand-written and IS formatted; everything below is emitted from it.
packages/tokens/src/zoblocks-tokens.css
packages/tokens/src/tailwind.css
packages/tokens/src/tokens.ts
packages/tokens/src/tokens.json
packages/tokens/src/contrast.json
packages/tokens/src/surface.ts

# Frozen. Everything under content/archive/ is a dated record of decisions made
# before the September 2026 rename; reformatting it would rewrite history for
# no reader's benefit. See content/archive/README.md.
content/archive
