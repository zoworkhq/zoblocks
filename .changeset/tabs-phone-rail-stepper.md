---
"@zoblocks/tabs": patch
---

Rail and stepper tabs now fit a phone. A rail narrower than 28rem stacks its list above the panel instead of drawing the panel over it; the tablist stays vertical, so arrow keys are unchanged. Stepper labels that do not fit end in an ellipsis rather than being cut mid-letter.
