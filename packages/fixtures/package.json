{
  "name": "@oxygenui/fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Synthetic, non-PHI FHIR fixtures for Oxygen docs, demos, and tests.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"[fixtures] consumed as source\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"[fixtures] lint: configured in Phase 1\""
  },
  "dependencies": {
    "@oxygenui/fhir": "workspace:*"
  },
  "devDependencies": {
    "@oxygenui/tsconfig": "workspace:*",
    "typescript": "^5.7.2"
  }
}
