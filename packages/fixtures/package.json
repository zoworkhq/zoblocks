{
  "name": "@zoblocks/fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Synthetic, non-PHI FHIR fixtures for ZoBlocks docs, demos, and tests.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"[fixtures] consumed as source\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zoblocks/fhir": "workspace:*"
  },
  "devDependencies": {
    "@zoblocks/tsconfig": "workspace:*",
    "typescript": "^7.0.2"
  }
}
