{
  "name": "@zoblocks/eslint-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Lint rules that enforce ZoBlocks's architectural invariants — the ones previously kept by comment.",
  "license": "MIT",
  "type": "module",
  "main": "./index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "build": "echo \"[eslint-plugin] plain ESM, no build\"",
    "typecheck": "echo \"[eslint-plugin] plain ESM, no typecheck\"",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --passWithNoTests --coverage"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
